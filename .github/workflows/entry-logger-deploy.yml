name: entry-logger-deploy
'on':
  workflow_dispatch:
    inputs:
      environment:
        description: Environment to deploy entry-logger to.
        required: true
        type: environment
      entry-logger-sha:
        description: Version of entry-logger to deploy.
        required: true
        type: string
      security-system-sha:
        description: Version of security-system to run upstream CDCs against.
        required: false
        type: string
      deployment-status-url:
        description: URL for status updates to be sent to.
        required: false
        type: string
jobs:
  Start:
    environment: ${{ github.event.inputs.environment }}
    needs: [
      ]
    steps:
    - uses: rsotnychenko/deployment-status-update@0.2.0
      name: Report deploy as in_progress
      with:
        run_id: ${{ github.run_id }}
        status: in_progress
        deployment_status_url: ${{ github.event.inputs.deployment-status-url }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{ github.event.inputs.deployment-status-url != null }}
    runs-on: ubuntu-latest
  CDC-security-system-to-entry-logger:
    needs:
    - Start
    steps:
    - run: echo run upstream CDCS here
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.security-system-sha != null }}
  Deploy-entry-logger:
    needs:
    - Start
    - CDC-security-system-to-entry-logger
    steps:
    - run: echo deploy entry-logger here
    runs-on: ubuntu-latest
    if: ${{ always()  && (needs.CDC-security-system-to-entry-logger.result == 'skipped'
      || needs.CDC-security-system-to-entry-logger.result == 'success') }}
  End:
    needs:
    - Deploy-entry-logger
    steps:
    - uses: rsotnychenko/deployment-status-update@0.2.0
      name: Report deploy as ${{ needs.Deploy-entry-logger.result }}
      with:
        run_id: ${{ github.run_id }}
        status: ${{ needs.Deploy-entry-logger.result }}
        deployment_status_url: ${{ github.event.inputs.deployment-status-url }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{ github.event.inputs.deployment-status-url != null }}
    runs-on: ubuntu-latest
    if: ${{ always() }}
