name: user-directory
'on':
  push:
    paths:
    - user-directory/**
    - common/**
    - .monopolis/config.yml
    - .github/workflows/app1-build.yml
jobs:
  Build:
    needs: [
      ]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: 11
      - run: ./gradlew :user-directory:check
    runs-on: ubuntu-latest
